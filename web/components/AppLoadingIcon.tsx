/** Shared loading image used across the app (matches app/loading.tsx and dashboard content loading). */
export default function AppLoadingIcon() {
  return (
    <img
      src="/loading-logo.png"
      alt=""
      className="app-loading-icon"
      width={80}
      height={40}
    />
  );
}
